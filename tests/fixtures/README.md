# Fixture registry

Fixtures must be synthetic, minimized, and free of real user data.

M0 contains no ChatGPT export fixture. M1 will add the first complete minimal export fixture under
this directory together with provenance and sanitization notes.
