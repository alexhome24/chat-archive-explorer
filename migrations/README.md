# Database migrations

Schema version `0` means no application database exists yet. M2 will introduce the migration
runner and migration `0001`; no hidden schema initialization is permitted before then.
